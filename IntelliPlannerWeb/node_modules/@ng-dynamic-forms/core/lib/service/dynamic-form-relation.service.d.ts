import { Injector } from "@angular/core";
import { FormControl, FormGroup } from "@angular/forms";
import { DynamicFormControlModel } from "../model/dynamic-form-control.model";
import { DynamicFormControlMatcher } from "./dynamic-form-relation.matchers";
import { DynamicFormControlCondition, DynamicFormControlRelation } from "../model/misc/dynamic-form-control-relation.model";
import { Subscription } from "rxjs";
export declare class DynamicFormRelationService {
    private DYNAMIC_MATCHERS;
    private injector;
    constructor(DYNAMIC_MATCHERS: DynamicFormControlMatcher[], injector: Injector);
    getRelatedFormControl(group: FormGroup, condition: DynamicFormControlCondition): FormControl | never;
    getRelatedFormControls(model: DynamicFormControlModel, group: FormGroup): {
        [id: string]: FormControl;
    } | never;
    findRelation(relations: DynamicFormControlRelation[], matcher: DynamicFormControlMatcher): DynamicFormControlRelation | null;
    matchesCondition(relation: DynamicFormControlRelation, group: FormGroup, matcher: DynamicFormControlMatcher): boolean;
    subscribeRelations(model: DynamicFormControlModel, group: FormGroup, control: FormControl): Subscription[];
}
